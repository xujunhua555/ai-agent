<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weeklyreport.mapper.SysPermissionMapper">
    <resultMap id="BaseResultMap" type="com.weeklyreport.entity.SysPermission">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="type" property="type"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_permission ORDER BY sort_order, id
    </select>
    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT * FROM sys_permission WHERE parent_id = #{parentId} ORDER BY sort_order, id
    </select>
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_permission WHERE id = #{id}
    </select>
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT * FROM sys_permission WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY sort_order, id
    </select>
    <select id="selectPermissionIdsByRoleId" resultType="long">
        SELECT permission_id FROM sys_role_permission WHERE role_id = #{roleId}
    </select>
    <select id="selectCodesByUserId" resultType="string">
        SELECT DISTINCT p.code FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>
</mapper>
